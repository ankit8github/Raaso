# Raaso Backend Environment Configuration
# Replace with your actual Supabase / PostgreSQL database connection URL
DATABASE_URL=postgresql+psycopg://user:password@host:port/database

# Allowed CORS origins (JSON array or comma-separated list depending on parser)
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000"]

